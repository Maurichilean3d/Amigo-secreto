🔐 CREDENCIALES DE SUPABASE - REDACTADAS POR SEGURIDAD
=========================================================

⚠️ Este archivo contenía credenciales reales (URL de Supabase, ANON KEY, SendGrid API KEY y un correo) expuestas en texto plano en un repositorio público. Han sido eliminadas de este archivo.

SUPABASE_URL=[ROTATED - ver gestor de secretos]
SUPABASE_ANON_KEY=[ROTATED - ver gestor de secretos]

CONFIGURACIÓN EN NETLIFY (variables de entorno, NO en este archivo):

SENDGRID_API_KEY=[ROTATED - configurar como variable de entorno en Netlify]
FROM_EMAIL=[configurar como variable de entorno en Netlify]

⚠️ IMPORTANTE:
Rota/revoca estas claves reales en los paneles de Supabase y SendGrid lo antes posible, ya que estuvieron expuestas públicamente. Configura las variables sensibles como variables de entorno en Netlify (Site settings > Environment variables), nunca en un archivo de texto dentro del repositorio.

Para el script SQL:
1. Ve a: https://supabase.com/dashboard
2. Abre tu proyecto
3. SQL Editor > New query
4. Copia y pega el contenido de: supabase-setup.sql
5. Click "Run"
